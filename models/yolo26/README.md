# YOLO26 Weights

Use the CLI download command to place the default YOLO26 pose checkpoint here:

```bash
forespin download yolo
```

Expected default path:

```text
/Users/james/playground/forespin/models/yolo26/yolo26n-pose.pt
```

The analyzer will use that file automatically if you do not pass `--player-pose-weights`.
