Place the learned tennis-court detector checkpoint in this directory.

Preferred default filename:

- `tennis_court_detector.pt`

If you keep a different filename, `forespin` will still pick it up automatically when this directory contains exactly one checkpoint file with a `.pt`, `.pth`, or `.bin` extension.

This model is local/manual only. It is not downloaded through the `forespin download` command.
